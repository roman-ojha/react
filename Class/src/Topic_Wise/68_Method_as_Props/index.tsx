import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

/*
  *) Event Handling:
    -> here we will see about how to handle event on react
    -> we will focus on 'click' event but the concept is same
 */

ReactDOM.render(
  <>
    <App />
  </>,
  document.getElementById("root")
);
